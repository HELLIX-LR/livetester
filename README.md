# LIVE RUSSIA Tester Dashboard

Веб-приложение для управления тестировщиками мобильного проекта LIVE RUSSIA с интеграцией Google Sheets, системой рейтинга и административной панелью.

## 🚀 Быстрый старт

### Автоматическая настройка (рекомендуется)
```bash
npm run quick-start
```

### Ручная настройка
```bash
# 1. Установка зависимостей
npm install

# 2. Настройка окружения
cp .env.example .env
# Отредактируйте .env с вашими настройками

# 3. Запуск миграций
npm run migrate

# 4. Создание администратора
npm run seed

# 5. Проверка системы
npm run checkpoint

# 6. Запуск сервера
npm run dev
```

## 📋 Требования

- **Node.js** 16.0+
- **PostgreSQL** 12.0+
- **Redis** 6.0+

## 🔧 Полезные команды

```bash
npm run setup        # Проверка и настройка окружения
npm run checkpoint   # Проверка работоспособности системы
npm run dev          # Запуск сервера разработки
npm run migrate      # Запуск миграций базы данных
npm run rollback     # Откат миграций (осторожно!)
npm run seed         # Создание администратора
npm test             # Запуск тестов
```

## 🌐 Доступ к системе

После запуска сервера:

- **Главная страница:** http://localhost:3000
- **Регистрация тестера:** http://localhost:3000/register.html
- **Страница входа:** http://localhost:3000/login.html
- **Админ панель:** http://localhost:3000/dashboard.html
- **Управление тестерами:** http://localhost:3000/testers.html
- **Управление багами:** http://localhost:3000/bugs.html
- **Статистика серверов:** http://localhost:3000/servers.html
- **Онлайн игроки:** http://localhost:3000/online-players.html
- **API Health:** http://localhost:3000/api/health

### Учетные данные по умолчанию
- **Username:** admin
- **Password:** admin123

⚠️ **Смените пароль после первого входа!**

## 📊 Основные функции

### ✅ Реализовано
- 🔐 Система авторизации администраторов
- 👥 Регистрация тестеров с валидацией
- 📊 API endpoints для управления данными
- 🗄️ База данных PostgreSQL с миграциями
- 🔄 Кэширование с Redis
- 📈 Интеграция с Google Sheets
- 🐛 Управление багами
- 💬 Система комментариев
- 📸 Загрузка скриншотов
- ⭐ Система рейтинга тестеров
- 📈 Статистика серверов (mock данные)
- 📊 Графики онлайн игроков (mock данные)
- 🔔 Система уведомлений
- 📄 Экспорт отчетов (CSV/PDF)
- 📱 Адаптивный frontend
- 🧪 Unit тесты для основных компонентов

### 🎯 Страницы
- 🏠 Dashboard - главная панель со статистикой
- 👥 Testers - управление тестерами
- 🐛 Bugs - управление багами
- 🖥️ Servers - статистика серверов
- 📊 Online Players - графики онлайн игроков
- 📝 Register - публичная регистрация тестеров

## 🗂️ Структура проекта

```
live-russia-tester-dashboard/
├── backend/
│   ├── config/          # Конфигурация (БД, Redis, Google)
│   ├── db/             # Миграции и скрипты БД
│   ├── middleware/     # Express middleware
│   ├── models/         # Модели данных
│   ├── routes/         # API маршруты
│   ├── services/       # Бизнес-логика
│   └── server.js       # Главный файл сервера
├── frontend/
│   ├── js/             # JavaScript файлы
│   ├── styles/         # CSS стили
│   └── *.html          # HTML страницы
├── uploads/            # Загруженные файлы
├── .env               # Переменные окружения
└── package.json       # Зависимости Node.js
```

## 🔧 Настройка Google Sheets (опционально)

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google Sheets API
3. Создайте OAuth 2.0 credentials
4. Настройте `backend/config/google-credentials.json`
5. Добавьте `GOOGLE_SHEETS_SPREADSHEET_ID` в `.env`

Подробные инструкции в [SETUP_GUIDE.md](SETUP_GUIDE.md)

## 🧪 Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск тестов в watch режиме
npm run test:watch

# Проверка системы
npm run checkpoint
```

## 📚 API Документация

### Тестеры
- `POST /api/testers` - Регистрация тестера
- `GET /api/testers` - Список тестеров (с фильтрами)
- `GET /api/testers/:id` - Детали тестера
- `PATCH /api/testers/:id` - Обновление тестера
- `PATCH /api/testers/:id/status` - Изменение статуса
- `GET /api/testers/top` - Топ-10 тестеров по рейтингу

### Баги
- `POST /api/bugs` - Создание бага
- `GET /api/bugs` - Список багов (с фильтрами)
- `GET /api/bugs/:id` - Детали бага
- `PUT /api/bugs/:id` - Обновление бага
- `DELETE /api/bugs/:id` - Удаление бага
- `POST /api/bugs/:id/screenshots` - Загрузка скриншота
- `POST /api/bugs/:id/comments` - Добавление комментария

### Авторизация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы
- `GET /api/auth/session` - Проверка сессии

### Серверы и Онлайн
- `GET /api/servers` - Статистика серверов
- `GET /api/online-players?period=24h` - Данные онлайн игроков

### Уведомления
- `GET /api/notifications` - Список уведомлений
- `GET /api/notifications/unread` - Непрочитанные уведомления
- `PATCH /api/notifications/:id/read` - Отметить как прочитанное

### Экспорт
- `GET /api/export/testers/csv` - Экспорт тестеров в CSV
- `GET /api/export/testers/pdf` - Экспорт тестеров в PDF
- `GET /api/export/bugs/csv` - Экспорт багов в CSV
- `GET /api/export/bugs/pdf` - Экспорт багов в PDF

### Google Sheets
- `GET /api/testers/google-sheets/status` - Статус подключения
- `GET /api/testers/google-sheets/load` - Загрузка из таблицы
- `POST /api/testers/google-sheets/sync` - Синхронизация

## 🐛 Устранение неполадок

### Ошибка подключения к PostgreSQL
```bash
# Проверьте, что PostgreSQL запущен
sudo systemctl status postgresql  # Linux
services.msc                      # Windows

# Проверьте настройки в .env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=live_russia_dashboard
DB_USER=postgres
DB_PASSWORD=your_password
```

### Ошибка подключения к Redis
```bash
# WSL/Linux
sudo service redis-server start

# Windows (если используете Redis для Windows)
# Запустите redis-server.exe
```

### Проблемы с миграциями
```bash
# Откат миграций (осторожно!)
npm run rollback

# Повторный запуск миграций
npm run migrate
```

## 📈 Мониторинг

- **Health Check:** `GET /api/health`
- **Логи:** Консоль сервера
- **Метрики:** Redis для кэширования статистики

## 🔒 Безопасность

- Пароли хешируются с bcrypt (10+ salt rounds)
- Сессии хранятся в Redis с TTL 24 часа
- CORS настроен для разработки
- Валидация всех входных данных
- Защита от SQL инъекций через параметризованные запросы

## 🤝 Разработка

### Добавление новых функций
1. Создайте миграцию в `backend/db/migrations/`
2. Добавьте модель в `backend/models/`
3. Создайте сервис в `backend/services/`
4. Добавьте маршруты в `backend/routes/`
5. Напишите тесты
6. Обновите frontend

### Стиль кода
- ESLint для JavaScript
- Prettier для форматирования
- JSDoc для документации
- Jest для тестирования

## 📄 Лицензия

Этот проект создан для LIVE RUSSIA mobile project.

## 🆘 Поддержка

При возникновении проблем:

1. Запустите `npm run setup` для диагностики
2. Проверьте логи в консоли
3. Убедитесь, что все сервисы запущены
4. Проверьте настройки в `.env`

---

**Удачи с разработкой! 🚀**