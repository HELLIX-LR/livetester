# LIVE RUSSIA Tester Dashboard - Руководство по настройке

## Требования к системе

- **Node.js** 16.0 или выше
- **PostgreSQL** 12.0 или выше  
- **Redis** 6.0 или выше
- **Git** для клонирования репозитория

## Быстрая установка (Windows)

### 1. Установка Node.js
```bash
# Скачайте и установите Node.js с официального сайта
# https://nodejs.org/en/download/
# Проверьте установку:
node --version
npm --version
```

### 2. Установка PostgreSQL
```bash
# Скачайте и установите PostgreSQL с официального сайта
# https://www.postgresql.org/download/windows/
# Во время установки запомните пароль для пользователя postgres
```

### 3. Установка Redis (Windows)
```bash
# Вариант 1: Использовать WSL2 (рекомендуется)
wsl --install
# После перезагрузки в WSL:
sudo apt update
sudo apt install redis-server
sudo service redis-server start

# Вариант 2: Использовать Redis для Windows (неофициальный)
# Скачайте с https://github.com/microsoftarchive/redis/releases
```

## Настройка проекта

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка базы данных PostgreSQL

#### Создание базы данных:
```sql
-- Подключитесь к PostgreSQL как пользователь postgres
psql -U postgres

-- Создайте базу данных
CREATE DATABASE live_russia_dashboard;

-- Создайте пользователя (опционально)
CREATE USER dashboard_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE live_russia_dashboard TO dashboard_user;

-- Выйдите из psql
\q
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=live_russia_dashboard
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Redis Configuration  
REDIS_URL=redis://localhost:6379

# Session Configuration
SESSION_SECRET=your_very_secure_session_secret_here

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Google Sheets Configuration (настроим позже)
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here
```

### 4. Запуск миграций базы данных
```bash
node backend/db/run-migrations.js
```

### 5. Создание администратора
```bash
node backend/db/seed-admin.js
```

По умолчанию создается пользователь:
- **Username:** admin
- **Password:** admin123

⚠️ **ВАЖНО:** Смените пароль после первого входа!

### 6. Запуск приложения
```bash
# Для разработки (с автоперезагрузкой)
npm run dev

# Для продакшена
npm start
```

## Проверка установки

### 1. Проверка подключений
```bash
# Проверка PostgreSQL
psql -U postgres -d live_russia_dashboard -c "SELECT NOW();"

# Проверка Redis
redis-cli ping
# Должен вернуть: PONG
```

### 2. Проверка приложения
```bash
# Запустите checkpoint тест
node checkpoint-verification.js
```

### 3. Ручная проверка
Откройте браузер и перейдите по адресам:
- http://localhost:3000 - главная страница
- http://localhost:3000/login.html - страница входа
- http://localhost:3000/dashboard.html - административная панель
- http://localhost:3000/api/health - проверка API

## Настройка Google Sheets (опционально)

### 1. Создание проекта в Google Cloud Console
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API

### 2. Создание учетных данных
1. Перейдите в "APIs & Services" > "Credentials"
2. Нажмите "Create Credentials" > "OAuth 2.0 Client IDs"
3. Выберите "Desktop application"
4. Скачайте JSON файл с учетными данными

### 3. Настройка учетных данных
```bash
# Создайте файл с учетными данными
cp backend/config/google-credentials.json.example backend/config/google-credentials.json
```

Отредактируйте файл, вставив данные из скачанного JSON.

### 4. Получение refresh token
```bash
# Запустите скрипт для получения токена
node backend/scripts/get-google-token.js
```

### 5. Создание Google Таблицы
1. Создайте новую Google Таблицу
2. Добавьте заголовки в первую строку:
   - A1: ID
   - B1: Name  
   - C1: Email
   - D1: Nickname
   - E1: Telegram
   - F1: Device Type
   - G1: OS
   - H1: OS Version
   - I1: Registration Date
   - J1: Status

3. Скопируйте ID таблицы из URL и добавьте в `.env`:
```env
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here
```

## Устранение неполадок

### Ошибка подключения к PostgreSQL
```bash
# Проверьте, что PostgreSQL запущен
# Windows:
services.msc # Найдите PostgreSQL и убедитесь, что он запущен

# Проверьте настройки подключения в .env
# Убедитесь, что пароль правильный
```

### Ошибка подключения к Redis
```bash
# Проверьте, что Redis запущен
# WSL:
sudo service redis-server status
sudo service redis-server start

# Windows (если используете Redis для Windows):
# Запустите redis-server.exe
```

### Ошибки миграций
```bash
# Проверьте права доступа к базе данных
psql -U postgres -c "GRANT ALL PRIVILEGES ON DATABASE live_russia_dashboard TO postgres;"

# Если нужно откатить миграции:
node backend/db/rollback-migrations.js
```

### Ошибки Google Sheets
```bash
# Проверьте настройки API в Google Cloud Console
# Убедитесь, что Google Sheets API включен
# Проверьте правильность учетных данных

# Тест подключения:
node -e "
const service = require('./backend/services/googleSheets.service');
service.checkConnection().then(console.log).catch(console.error);
"
```

## Команды для разработки

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Запуск тестов
npm test

# Запуск миграций
npm run migrate

# Создание админа
npm run seed

# Проверка checkpoint
node checkpoint-verification.js

# Откат миграций (осторожно!)
node backend/db/rollback-migrations.js
```

## Структура проекта

```
live-russia-tester-dashboard/
├── backend/
│   ├── config/          # Конфигурация БД, Redis, Google
│   ├── db/             # Миграции и скрипты БД
│   ├── middleware/     # Express middleware
│   ├── models/         # Модели данных
│   ├── routes/         # API маршруты
│   ├── services/       # Бизнес-логика
│   └── server.js       # Главный файл сервера
├── frontend/
│   ├── js/             # JavaScript файлы
│   ├── styles/         # CSS стили
│   ├── index.html      # Главная страница
│   ├── login.html      # Страница входа
│   └── dashboard.html  # Административная панель
├── uploads/            # Загруженные файлы
├── .env               # Переменные окружения
├── package.json       # Зависимости Node.js
└── README.md          # Документация
```

## Следующие шаги

После успешной настройки:

1. **Протестируйте регистрацию тестера** через API
2. **Войдите в административную панель** (admin/admin123)
3. **Проверьте синхронизацию с Google Sheets** (если настроено)
4. **Запустите полный набор тестов**
5. **Перейдите к разработке следующих функций**

## Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все сервисы запущены
3. Проверьте настройки в `.env`
4. Запустите checkpoint тест для диагностики

Удачи с настройкой! 🚀